var BitSize = (function () {
    'use strict';

    _BitSize.$inject = [];

    function _BitSize() {
        return {
        };
    }

    return new _BitSize();        // TODO change it to dependency injection

})();
